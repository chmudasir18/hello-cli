# hello-cli

A tiny command-line greeter, used as a practice repo for learning the GitHub PR workflow.

## Usage

```bash
python greet.py Ada
# Hello, Ada!
```

This is a simple libary with one function, `greet(name)`, that returns a friendly greeting.

## Tests

```bash
pytest
```
